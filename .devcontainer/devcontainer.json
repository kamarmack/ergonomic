{
	"build": {
		"dockerfile": "../Dockerfile",
		"context": ".."
	},
	"customizations": {
		"vscode": {
			"settings": {
				"terminal.integrated.shell.linux": "/bin/bash"
			}
		}
	},
	"forwardPorts": [],
	"name": "Ergonomic Dev Container",
	"postStartCommand": "git config --global core.autocrlf input && npm install"
}
